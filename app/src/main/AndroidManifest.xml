<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.enciclopediadeportiva">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_olympic_games"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_olympic_games_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListaDeportesVerano"></activity>
        <activity android:name=".ListaDeportesInvierno" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>